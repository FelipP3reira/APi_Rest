# Copie para .env e ajuste o que precisar.
NODE_ENV=development
PORT=3333

# Postgres do docker-compose deste repositório (host na 5433 pra evitar conflito).
DATABASE_URL=postgresql://locadora:locadora@localhost:5433/locadora?schema=public
